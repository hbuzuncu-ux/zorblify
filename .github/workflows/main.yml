name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 10 */4 * *'  # Her 4 günde bir saat 10:00'da çalışır
  workflow_dispatch:         # Manuel çalıştırma için

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            "https://lscqqfeqpbfmcccifrwj.supabase.co/rest/v1/scores?limit=1" \
            -H "apikey: sb_publishable_HPGXHwa51dk_1nmAqoeoXA_6zCzOVzM" \
            -H "Authorization: Bearer sb_publishable_HPGXHwa51dk_1nmAqoeoXA_6zCzOVzM"
          echo " — Supabase pinged successfully"
